<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jt.icaew.android"
    android:versionCode="5"
    android:versionName="0.0.92">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppCustomTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".activity.program.detail.ProgramDetailActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".activity.contact.detail.ContactDetailActivity"
            android:theme="@style/SplashTheme" />
        <activity android:name=".activity.events.detail.EventListActivity" />
        <activity android:name=".activity.universities.detail.UniversityDetailActivity" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="00fd18220f391f657cb53b7830f04e5efd103e49" />

        <activity android:name=".activity.events.detail.EventDetailActivity"></activity>
    </application>

</manifest>